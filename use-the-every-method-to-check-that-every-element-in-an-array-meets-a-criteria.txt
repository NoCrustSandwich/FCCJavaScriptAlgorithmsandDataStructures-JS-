function checkPositive(arr) {
  // Only change code below this line

  return arr.every(positiveCheck);

  // Only change code above this line
}

checkPositive([1, 2, 3, -4, 5]);

function positiveCheck(val)
{

  if(val>0)
  {

    return true;

  }

  return false;

}